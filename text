header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px;
  background-color: #333;
  position: relative;
}

.log {
  width: 50px;
  height: 60px;
}

.logo {
  color: #fff;
  font-size: 24px;
}

.nav-links {
  display: flex;
  list-style: none;
  flex-direction: row;
  position: static;
  width: auto;
  background-color: transparent;
  justify-content: flex-end;
  margin: 0;
  padding: 0;
}

/* Submenu სტილები */
.submenu {
  display: none;
  position: absolute;
  top: 100%;
  left: 0;
  background-color: #444;
  list-style: none;
  padding: 10px;
  width: 150px;
  z-index: 100;
  margin: 0; /* დარწმუნდით, რომ აქ არ ხდება შიდა სივრცის შეცვლა */
}

.submenu-parent.active .submenu {
  display: block;
}

.submenu {
  display: none; /* დაწყებული იქნება დამალული */
}

.submenu-parent.active .submenu {
  display: block; /* submenu გამოჩნდება, როცა parent-ს აქვს "active" კლასი */
}

.nav-links li {
  margin-left: 20px;
  position: relative;
}

.nav-links a {
  color: white;
  text-decoration: none;
  font-size: 18px;
}

ul {
  z-index: 1000;
}

/* Submenu */
.submenu {
  display: none;
  position: absolute;
  top: 100%;
  left: 0;
  background-color: #444;
  list-style: none;
  padding: 10px;
  width: 150px;
  z-index: 1000;
}

.submenu li {
  margin-bottom: 10px;
}

.submenu a {
  color: white;
  text-decoration: none;
}

.submenu-parent:hover .submenu {
  display: block;
}

.burger {
  display: none;
  cursor: pointer;
  flex-direction: column;
  gap: 5px;
}

.burger div {
  width: 25px;
  height: 3px;
  background-color: white;
}












.burger {
  margin-right: 30px;
  transform: translateY(30px);
  display: none;
  cursor: pointer;
  flex-direction: column;
  gap: 5px;
}

.burger div {
  margin-left: auto;
  width: 25px;
  height: 3px;
  background-color: white;
}

/* Mobile styles */
@media screen and (max-width: 768px) {
  .nav-links {
    display: none;
    flex-direction: column;
    width: 100%;
    text-align: center;
    position: absolute;
    top: 70px;
    right: 0;
    background-color: #000000;
    box-shadow: 0px 10px 5px rgb(255, 0, 0);
  }

  .nav-links.active {
    display: flex;
  }

  .burger {
    display: flex;
  }

  .nav-links li {
    margin: 15px 0;
  }

  /* Submenu mobile */
  .submenu {
    margin: auto;
  }

  .submenu-parent .submenu {
    padding: 60px;
    display: none;
    position: static;
    background-color: #000000;
    padding: 10px;
  }

  .submenu-parent.active .submenu {
    display: flex;
    flex-direction: column;
  }
}      